import React from 'react';
import BuyCompleted from './BuyCompleted';
import SellCompleted from './SellCompleted';

const RequestCompleted = () => {
    return (
        <>
        <SellCompleted/>
        <BuyCompleted/>
        </>
    );
};

export default RequestCompleted;