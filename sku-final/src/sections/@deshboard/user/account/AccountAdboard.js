import React from 'react';
import { Box, Card, Typography } from '@mui/material';

export default function AccountAdboard () {
  return (
    <Card sx={{ p: 3 }}>
      <Typography variant='subtitle1' sx={{pb: 2}}>Adboard Setup & billing</Typography>
      </Card>
)}
